#include <stdio.h>

void main()
{
    // int a = 7;
    // int b = 2;
    //
    // printf("divisao inteira: %d \n", (a / b));
    // printf("resto: %d \n", a % b);
    // printf("divisao real %.2f", (float)a/b);

    char letra = 'a';
    letra = letra + 5;
    printf("%c", letra);
}
